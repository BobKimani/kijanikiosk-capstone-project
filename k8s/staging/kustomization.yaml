apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kijani-staging

resources:
  - namespace.yaml
  - ../base
  - configmap.yaml

images:
  - name: kk-payments
    newName: kk-payments
    newTag: staging