apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kijani-production

resources:
  - namespace.yaml
  - ../base
  - configmap.yaml

images:
  - name: kk-payments
    newName: kk-payments
    newTag: production